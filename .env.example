DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"
CRON_SECRET="generate-a-long-random-string"
SESSION_SECRET="generate-another-long-random-string"
NEXT_PUBLIC_APP_URL="https://job-hunt-copilot-blush.vercel.app"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:you@example.com"

# Optional — LLM (also configurable per-user in Settings)
OPENAI_API_KEY=""

# Optional — contact enrichment
HUNTER_API_KEY=""

# Optional — job boards (free/public keys)
ADZUNA_APP_ID=""
ADZUNA_APP_KEY=""
USAJOBS_API_KEY=""
USAJOBS_USER_AGENT="your-registered-email@example.com"

# Optional — no paid ATS vendor keys are used.
# ATS scoring is heuristic (keyword + structure). If you later add a free public ATS API,
# document its key here; do not commit secrets.
